# The core local pipeline uses only the Python standard library.
# Optional packages for the advanced Hugging Face transformer step.
transformers
torch

# Optional packages for AWS/SageMaker implementation.
boto3
sagemaker
